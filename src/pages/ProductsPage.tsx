import { useCallback,useEffect,useMemo,useState } from 'react'
import { Archive,Edit,Plus,Search } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { Category,Product } from '../types/database'
import { archiveProduct,listCategories,listProducts,productImageUrl,setAvailability } from '../services/products'
import { formatMoney } from '../utils/money'
export function ProductsPage(){const [products,setProducts]=useState<Product[]>([]);const [categories,setCategories]=useState<Category[]>([]);const [query,setQuery]=useState('');const [categoryId,setCategoryId]=useState<string|null>(null);const [error,setError]=useState('');const load=useCallback(async()=>{try{const [loadedProducts,loadedCategories]=await Promise.all([listProducts(true),listCategories(false)]);setProducts(loadedProducts);setCategories(loadedCategories);setCategoryId(current=>current??loadedCategories[0]?.id??null)}catch{setError('Unable to load products.')}},[]);useEffect(()=>{void load()},[load]);const filtered=useMemo(()=>products.filter(product=>(!categoryId||product.category_id===categoryId)&&product.name.toLowerCase().includes(query.toLowerCase())),[products,categoryId,query]);async function archive(p:Product){if(!confirm(`Archive ${p.name}? Historical order data will remain unchanged.`))return;try{await archiveProduct(p.id);await load()}catch{setError('Unable to archive this product.')}}return <div className="p-4 pt-16 lg:p-8"><div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-bold">Products</h1><p className="text-sm text-stone-500">Manage menu items and availability.</p></div><Link to="/products/new" className="flex items-center gap-2 rounded-xl bg-brand-500 px-4 py-3 font-semibold text-white"><Plus size={19}/>Add product</Link></div>{error&&<p role="alert" className="mb-4 rounded-xl bg-red-50 p-3 text-red-700">{error} <button onClick={load} className="font-bold">Retry</button></p>}<div className="mb-5 flex flex-wrap items-center gap-2"><label className="mr-2 flex w-full items-center gap-2 rounded-xl border bg-white px-3 sm:w-72"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search products" className="w-full py-3 outline-none"/></label>{categories.map(category=><button key={category.id} type="button" aria-pressed={categoryId===category.id} onClick={()=>setCategoryId(category.id)} className={`rounded-full px-4 py-2 text-sm font-semibold ${categoryId===category.id?'bg-brand-900 text-white':'border border-stone-200 bg-white text-stone-700 hover:border-brand-500'}`}>{category.name}</button>)}</div><div className="overflow-x-auto rounded-2xl bg-white shadow-sm"><table className="w-full min-w-[620px] text-left"><thead className="border-b bg-stone-50 text-xs uppercase text-stone-500"><tr><th className="p-4">Product</th><th>Price</th><th>Status</th><th className="p-4 text-right">Actions</th></tr></thead><tbody>{filtered.map(p=><tr key={p.id} className={`border-b last:border-0 ${!p.is_active?'opacity-50':''}`}><td className="p-4"><div className="flex items-center gap-3">{productImageUrl(p.image_path)?<img src={productImageUrl(p.image_path)!} className="h-12 w-12 rounded-lg object-cover" alt=""/>:<div className="h-12 w-12 rounded-lg bg-stone-100"/>}<div><strong>{p.name}</strong>{!p.is_active&&<p className="text-xs text-red-600">Archived</p>}</div></div></td><td>{p.unit_type==='kilogram'?'Tiered':formatMoney(p.price_centavos??0)}</td><td><button disabled={!p.is_active} onClick={async()=>{await setAvailability(p.id,!p.is_available);await load()}} className={`rounded-full px-3 py-1 text-xs font-bold ${p.is_available?'bg-emerald-100 text-emerald-700':'bg-stone-200 text-stone-600'}`}>{p.is_available?'AVAILABLE':'SOLD OUT'}</button></td><td className="p-4"><div className="flex justify-end gap-2"><Link to={`/products/${p.id}/edit`} className="rounded-lg border p-2" aria-label={`Edit ${p.name}`}><Edit size={17}/></Link>{p.is_active&&<button onClick={()=>void archive(p)} className="rounded-lg border p-2 text-red-600" aria-label={`Archive ${p.name}`}><Archive size={17}/></button>}</div></td></tr>)}</tbody></table></div></div>}
